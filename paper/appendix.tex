%% =========================================================================
\appendix

\section{Appendix}

\subsection{History and Computational Status}
\label{app:history}
%% =========================================================================

Mullin posed the conjecture in 1963~\cite{Mullin1963}.  In over sixty
years, no proof has been found, and no theoretical approach has come
close.  The problem sits in an unusual position: it is elementary to
state, each individual step is deterministic, yet the global behavior
of the sequence appears completely intractable.

\paragraph{The largest-factor variant.}
Cox and van der Poorten~\cite{CoxVdP1968} showed that the related sequence using
the \emph{largest} prime factor---where each term is
$\operatorname{gpf}(\Prod{2}(n) + 1)$ instead of $\minFac$---misses
specific primes (for instance,~$5$ never appears) and conjectured that
it misses infinitely many; Booker~\cite{Booker2012} proved the
infinitude.  By
always jumping to the largest factor of $\Prod{2}(n) + 1$, the sequence
leaps past small primes and can never return to them, since each Euclid
number is coprime to every earlier term.  This refuted the natural
strengthening that surjectivity holds regardless of the factor-selection
rule, and showed that the $\minFac$ rule is essential to the conjecture.

The two selection rules are also structurally different.  The largest
prime factor $P^+(n) = \operatorname{gpf}(n)$ has rich algebraic
structure: the sets $\{n : P^+(n) \leq y\}$ (smooth numbers) are
controlled by the Dickman function, and for polynomial sequences
$f(n) = \Prod{2}(n)+1$ one can sometimes exploit algebraic curves and
Hasse--Weil bounds.  In contrast, the smallest prime factor
$P^-(n) = \minFac(n)$ is purely a \emph{sieve} object: controlling
$P^-(n)$ requires excluding small prime divisors one at a time (CRT
$+$ Mertens), and no algebraic-geometric handle exists.  This
$\operatorname{gpf}$/$\minFac$ asymmetry is one reason the largest-factor
variant admits a short proof while the smallest-factor conjecture
remains open.

\paragraph{The walk heuristic.}
Shanks~\cite{Shanks1991} was the first to articulate the heuristic
argument underlying the conjecture: if~$q$ is the smallest prime that
never appears, the walk $\Prod{2}(n) \bmod q$ in $(\ZZ/q\ZZ)^\times$
is well-defined for all~$n$, and under a pseudo-randomness assumption
it must eventually hit~$-1$.  The formalization's CME and CCSB
(Sections~\ref{sec:character}--\ref{sec:ensemble}) are rigorous
formulations of this heuristic: each specifies the precise sense in
which ``the walk behaves pseudo-randomly'' would suffice.

\paragraph{Variants and surveys.}
Booker~\cite{BoSa2012} showed that a carefully chosen variant of the
Euclid--Mullin sequence \emph{does} contain every prime: by selecting a
specific (not necessarily smallest) prime factor at each step, one can
steer the sequence to hit every prime.  This demonstrates that the
conjecture is \emph{delicate}: the surjectivity depends on the precise
rule, not just the Euclidean structure.  Pollack and
Trevi\~{n}o~\cite{PollackTrevino2014} surveyed the problem's place in
the broader landscape of Euclid-inspired sequences, and studied
distributional properties of primes ``forgotten'' by Euclid-type
constructions.  Most recently, Booker and
Simon~\cite{BoSi2026} generalized the framework to primes in
arithmetic progressions via cyclotomic polynomials~$\Phi_m(cx)$,
proving (under ERH, and unconditionally for $m \leq 18$) that the
maxFac variant omits infinitely many primes $p \equiv 1 \pmod{m}$.

\paragraph{Computational status.}
The sequence has been extended through a series of large-scale
factoring efforts:
\begin{itemize}[nosep]
\item Wagstaff~(1993) computed through the 43rd term.
\item In 2010, the 180-digit number $\Prod{2}(43) + 1$ was factored via
  GNFS (General Number Field Sieve), yielding a 68-digit prime as
  $a(44)$.  Terms $a(45)$--$a(47)$ followed.
\item In 2012, Propper factored the 256-digit number $\Prod{2}(47)+1$ by
  ECM (Elliptic Curve Method), discovering a 75-digit factor and
  extending the sequence to 51~terms
  (Booker--Irvine~\cite{BoIr2016}).
\item Finding $a(52)$ requires factoring a 335-digit number.  No
  factorization is known as of 2025.
\end{itemize}
After 51~terms, the smallest primes not yet observed are $41$ and $47$.
Note that $31$---a smaller prime---does not appear until position~$50$.

\paragraph{Brute force and the complexity of factoring.}
Brute-force computation cannot attack the conjecture.  Each new term
requires factoring a number whose digit count grows roughly linearly
with the number of terms, quickly exceeding the reach of any known
algorithm.  But even if we could compute millions of terms, the
conjecture is a $\forall$-statement over all primes, and no finite
prefix can rule out a prime that first appears at an astronomically
large index.

The deeper issue is a possible link to the computational complexity of
integer factorization.  Factoring is believed to be \emph{slightly
less than hard}: neither polynomial (as it would be if $\mathsf{P} =
\mathsf{NP}$) nor fully exponential, but subexponential---the best
known algorithms (number field sieve) run in time
$L_N(1/3,\,c) = \exp\bigl(c\,(\ln N)^{1/3}\,(\ln\ln N)^{2/3}\bigr)$.
This intermediate complexity may be reflected in the conjecture
itself.  Each term $a(n{+}1) = \minFac(\Prod{2}(n)+1)$ depends on the
\emph{complete factorization} of a number encoding all previous terms;
changing a single early term alters every subsequent one.  If factoring
were easy, the sequence would be transparently computable and the
conjecture might yield to direct analysis.  If factoring were
maximally hard (truly exponential), the sequence would behave like a
random walk and the conjecture could be studied probabilistically.
The conjectured intermediate complexity of factoring places the
Euclid--Mullin sequence in a regime where it has enough algebraic
structure to support a proof (it is not random), yet resists brute
computation---precisely the regime addressed by the structural approach
in the body of this paper.

%% =========================================================================
\subsection{Analogies and Context}
\label{app:analogies}
%% =========================================================================

Mullin's Conjecture has no known applications: if proved tomorrow, no
other theorem in number theory would follow from it.  The value of the
problem lies instead in what it \emph{is an instance of}---and in the
methods its resolution would require.  The orbit-specificity barrier
identified by this formalization appears, in recognizable form, across
several active areas of mathematics.

\paragraph{Artin's conjecture and the orbit-specificity gap.}
The closest structural analogue to MC is Artin's conjecture on
primitive roots~\cite{Artin1927}: for any integer $a \neq -1$ that is not a
perfect square, the group $(\ZZ/p\ZZ)^\times$ is generated by~$a$ for
infinitely many primes~$p$.  The parallel is precise:
\begin{itemize}[nosep]
\item \textbf{Artin} asks whether the orbit of a fixed generator~$a$
  under repeated multiplication fills $(\ZZ/p\ZZ)^\times$.
\item \textbf{Mullin} asks whether the orbit of~$2$ under
  multiplication by successive EM primes in $(\ZZ/q\ZZ)^\times$ hits
  the single element~$-1$.
\end{itemize}
Both conjectures are blocked by the same fundamental obstacle:
transferring \emph{averaged} equidistribution results (which hold for
most moduli or most generators) to \emph{one specific deterministic
orbit}.  Hooley~\cite{Hooley1967} proved Artin's conjecture conditional on GRH
for Dedekind zeta functions of Kummer extensions---because GRH
provides the uniformity across individual characters needed to control
a single orbit.  In our setting, this uniformity is exactly what CCSB
demands.

The analogy is not merely structural.  The Kummer extensions
$\mathbb{Q}(\zeta_\ell, a^{1/\ell})$ that appear in Hooley's proof
are the same extensions that arise in the EffectiveKummerEscape
approach to SubgroupEscape (Section~\ref{sec:open}).  The elementary
PRE lemma (Theorem~\ref{thm:pre}) sidesteps this Chebotarev machinery
entirely for the algebraic component, but the dynamical
component---does the walk \emph{hit}~$-1$, not merely \emph{generate}
the full group?---remains exactly the orbit-specificity gap that GRH
closes for Artin and that no known tool closes for Mullin.

\paragraph{Multiplicative walks on finite groups.}
The walk reformulation (Section~\ref{sec:walk}) places MC in the
framework of random walks on finite groups, studied systematically by
Diaconis and others since the
1980s~\cite{ChungDiaconisGraham1987}.  The Diaconis--Shahshahani upper
bound lemma~\cite{DiaconisShahshahani1981} shows that a random walk on a finite group~$G$
driven by i.i.d.\ multipliers from a conjugation-invariant
distribution mixes in $O(\log |G|)$ steps, with mixing measured by
character sums.

The EM walk has the same algebraic structure---a multiplicative walk
on the cyclic group $(\ZZ/q\ZZ)^\times$---but violates every
assumption of the classical mixing theory.  The multipliers are
deterministic, not random; they are not identically distributed; and
most critically, the multiplier at step~$n$ depends on the walk
position at step~$n$, creating exactly the position-multiplier
correlation that the Diaconis--Shahshahani framework assumes away.
CCSB is the precise derandomization of the mixing-time bound: it asks
that the Fourier coefficients of the walk's occupation measure tend to
zero for every non-trivial character.

\paragraph{Smallest prime factor distribution.}
The smallest prime factor function
$P^-(n) = \min\{p : p \mid n\}$ was studied by Alladi~\cite{Alladi1977},
Hildebrand~\cite{Hildebrand1986}, and others.  Its distribution is
dominated by small primes: the density of $\{n : P^-(n) = p\}$ is
$p^{-1}\prod_{r<p}(1-1/r)$, so $P^-(n) \bmod q$ is \emph{not}
equidistributed for any fixed~$q$ (Dead End~\#160); Alladi's theorem
is the M\"obius-weighted duality
$-\sum_{n \le x,\, P^-(n) \equiv a} \mu(n) \sim x/\varphi(q)$, not an
unweighted equidistribution.  An earlier draft's SieveTransfer
hypothesis rested on the unweighted reading and is retired.
MC asks about the residue of $P^-$ at the specific values
$\{\Prod{2}(n)+1\}_{n \geq 0}$, where the least prime factor is
outside the bag and eventually large.
The same orbit-specificity transfer problem arises for Mersenne
numbers $2^p - 1$, Fibonacci numbers, and polynomial iterates
$f^{\circ n}(a)$ in arithmetic dynamics.

\paragraph{The marginal/joint barrier and Sarnak's conjecture.}
The formalization identifies a precise meta-obstacle
(Section~\ref{sec:hard}): \emph{marginal} equidistribution of the
multiplier residues is provable (the EM primes are equidistributed in
residue classes, by Dirichlet's theorem); what MC requires is
\emph{joint} equidistribution of the pair (walk position, multiplier),
conditioned on the walk's history.  This barrier is an instance of a
broader phenomenon.  Sarnak's conjecture~\cite{Sarnak2010} asserts that the
M\"obius function $\mu(n)$ is orthogonal to every bounded
deterministic sequence:
$\frac{1}{N} \sum_{n \leq N} \mu(n) \, a_n \to 0$.
CCSB is a M\"obius-orthogonality-type statement for the EM sequence,
placing MC squarely within the Sarnak program's conceptual framework,
even though the EM sequence falls outside the technical scope of
existing results (which require zero topological entropy).

\paragraph{Greedy sieves and orbit-hitting.}
MC is the simplest nontrivial instance of a broader question: does a
greedy, deterministic prime-selection process eventually cover all
primes?  The Cox--van der Poorten result~\cite{CoxVdP1968} shows that this
determinism is fragile: choosing the \emph{largest} factor instead
provably misses primes.  More generally, MC belongs to the family of
\emph{orbit-hitting problems} in arithmetic dynamics: given a map~$T$
on a space~$X$ and a target set $S \subset X$, does the orbit
eventually enter~$S$?  Unlike Artin (where the map $x \mapsto ax$ is
the same at every step) or Collatz (where the map depends only on the
current state), the EM map varies at each step, determined by the
factorization of a number that depends on the entire orbit history.
This is the accumulator coupling of Section~\ref{sec:intro}: the
running product $\Prod{2}(n)$ is a cumulative digest of the full orbit,
and the walk--multiplier framework makes it precise.  The
formalization shows it is the sole source of difficulty: once the
coupling is controlled (via CCSB or CME), MC follows by
machine-checked deduction.

%% =========================================================================
\subsection{Additional Sieve and Spectral Routes}
\label{app:routes}
%% =========================================================================

This appendix collects the sieve and spectral-energy routes to MC that
complement the three principal reductions (DH, CCSB, BV) presented in
the body.  All reduction arrows are machine-verified; the sole open
content in each route is the orbit-specificity transfer.

\subsubsection*{Arithmetic Large Sieve Route}

\begin{theorem}[\lean{EM/LargeSieve/Basic.lean}{arith\_ls\_chain\_mc}]
$\mathrm{ArithLS} + \mathrm{ArithLSImpliesMMCSB} \;\Longrightarrow\; \MC$.
\end{theorem}
The arithmetic large sieve gives character sum bounds for Dirichlet
characters (a known result, not in Mathlib as of \texttt{v4.33.0}).  The transfer
ArithLSImpliesMMCSB is open and is in fact a \textbf{dead
end}: universal coefficient bounds cannot distinguish equidistributed
walks from clumped walks.

\subsubsection*{Analytic Large Sieve Route}

The most developed route connects the analytic large sieve to MC via
Gauss sum inversion.

\begin{property}[\defname{AnalyticLargeSieve} (\abbr{ALS})]
For well-separated points $\{\alpha_r\} \subset \mathbb{R}/\mathbb{Z}$
with $\min_{r \neq s} \|\alpha_r - \alpha_s\| \geq \delta$:
\[
\sum_r \left\|\sum_{n < N} a_n\, e(n\alpha_r)\right\|^2
\;\leq\; (N - 1 + \delta^{-1}) \sum_{n < N} \|a_n\|^2.
\]
\end{property}

\begin{theorem}[\lean{EM/LargeSieve/Analytic.lean}{weak\_als\_from\_card\_bound}]
\label{thm:weak-als}
A \emph{weak} version with constant $N \cdot (\delta^{-1} + 1)$ is
proved (the optimal constant is $N - 1 + \delta^{-1}$, but the
difference is immaterial since MMCSB requires only~$o(N)$).
\end{theorem}

The key bridge is \defname{Gauss sum inversion}: a Gauss sum
$\tau(\chi) = \sum_{a} \chi(a)\, e(a/p)$ intertwines multiplication
and addition on~$\ZZ/p\ZZ$, converting character sums to exponential
sums.

\begin{theorem}[\lean{EM/LargeSieve/Analytic.lean}{char\_sum\_to\_exp\_sum}]
\label{thm:gauss-inversion}
For a non-trivial character~$\chi$ mod~$p$ prime:
$\sum_n f(n)\, \chi(n)
= \tau^{-1} \sum_{b=1}^{p-1} \chi^{-1}(b) \sum_n f(n)\, \psi(bn)$.
\end{theorem}

The GaussConductorTransfer composes eight internal lemmas (all proved,
\S56--\S62) into the bridge from ALS to the prime arithmetic large sieve:

\begin{theorem}[\lean{EM/LargeSieve/PrimeArithLS.lean}{als\_implies\_prime\_arith\_ls}]
\label{thm:als-prime}
$\mathrm{AnalyticLargeSieve} \;\Longrightarrow\; \mathrm{PrimeArithLS}$.
\end{theorem}

\begin{theorem}[\lean{EM/LargeSieve/PrimeArithLS.lean}{als\_prime\_arith\_ls\_chain\_mc}]
$\mathrm{ALS} + \mathrm{PrimeArithLSImpliesMMCSB} \;\Longrightarrow\; \MC$.
\end{theorem}

A deeper decomposition of ALS itself is architectured in
\code{EM/IK/Ch7Hilbert.lean}: the \textbf{Hilbert chain}
\[
\mathrm{HilbertInequality} + \mathrm{CscPartialFraction}
\;\xrightarrow{\text{open}}\;
\mathrm{CscBilinearBound}
\;\xrightarrow{\text{open}}\;
\mathrm{GramOffDiagBilinearBound}
\;\xrightarrow{\text{proved}}\;
\mathrm{ALS}
\]
(\lean{EM/IK/Ch7Hilbert.lean}{hilbert\_chain\_als}).
\defname{HilbertInequality} is the Montgomery--Vaughan
generalized Hilbert inequality (IK Lemma~7.8);
\defname{CscPartialFraction} is the Mittag-Leffler expansion
$\pi/\sin(\pi\theta) = 1/\theta + \sum_{n\geq 1}(-1)^n(1/(\theta{+}n) + 1/(\theta{-}n))$,
derivable from Mathlib's \code{cot\_series\_rep} via $\csc x = \cot(x/2) - \cot x$.
The infrastructure for CscPartialFraction (the identity
$\pi\cot(\pi\theta/2) - \pi\cot(\pi\theta) = \pi/\sin(\pi\theta)$
and both Mathlib cot-series specializations) is proved; the final
\code{HasSum} assembly is complete (proved via
\code{mittag\_leffler\_csc\_proved}).
Two bridges (\code{HilbertCscBilinearBridge},
\code{CscBilinearImpliesGramOffDiag}) and the terminal arrow
(\code{gram\_offdiag\_bilinear\_implies\_als}) complete the chain.

The remaining open content is PrimeArithLSImpliesMMCSB: transferring
prime-modulus arithmetic large sieve bounds to multi-modular character
sum bounds for the specific EM orbit.

\subsubsection*{The Spectral Energy Route}
\label{sec:sve}

Instead of individual character sums, this route examines the
\emph{total energy} of the walk occupation measure
$V_N(a) = |\{n < N : \walkZ{2}{q}{n} = a\}|$.

\begin{theorem}[\lean{EM/LargeSieve/Spectral.lean}{walk\_energy\_parseval}]
\label{thm:walk-energy}
$\sum_\chi \|S_\chi(N)\|^2 = (q{-}1) \sum_{a \in (\ZZ/q\ZZ)^\times} V_N(a)^2$
\quad (Parseval).
\end{theorem}

The \emph{excess energy} $E(N) = \sum_{\chi \neq 1} \|S_\chi(N)\|^2$.
If $E(N) = o(N^2)$, then every non-trivial character sum is
individually~$o(N)$, which is CCSB.

\begin{property}[\defname{SubquadraticVisitEnergy} (\abbr{SVE})]
For every missing prime~$q$ and $\varepsilon > 0$, there exists
$N_0$ such that for $N \geq N_0$: $E(N) \leq \varepsilon N^2$.
\end{property}

\begin{theorem}[\lean{EM/LargeSieve/Spectral.lean}{sve\_implies\_mmcsb}]
\label{thm:sve-mc}
$\mathrm{SVE} \Longrightarrow \mathrm{MMCSB} \Longrightarrow \MC$.
\end{theorem}

\noindent SVE bounds the total energy $\sum_{\chi \neq 1} \|S_\chi(N)\|^2
= o(N^2)$; since each non-trivial character sum is a non-negative
summand, every individual $\|S_\chi(N)\|^2 = o(N^2)$, giving CCSB for
each prime individually and MMCSB jointly.

\paragraph{Van der Corput and higher-order decorrelation.}
The van der Corput inequality bounds $|\sum z_n|$ via autocorrelations:

\begin{theorem}[Discrete van der Corput;
\lean{EM/ForMathlib/VanDerCorput.lean}{van\_der\_corput\_bound}]
\label{thm:vdc}
Let $\|z_n\| \leq 1$, let $1 \leq H \leq N$ and $\delta > 0$, and suppose
$\bigl\|\sum_{n < N-h} z_n \overline{z_{n+h}}\bigr\| \leq \delta N$ for
every $1 \leq h \leq H$.  Then
\[
  \Bigl\|\sum_{n < N} z_n\Bigr\|^2
  \;\leq\; \frac{2N^2}{H+1} \;+\; 2\delta N^2 .
\]
\end{theorem}

\noindent This is the \emph{discrete} (Weyl--van der Corput) inequality,
in the weakened corollary form in which it is used here (the constants of
Iwaniec--Kowalski relaxed); it is unrelated to the oscillatory-integral
van der Corput lemma, and is not in Mathlib as of \texttt{v4.33.0}.

For the EM walk, lag-$h$ autocorrelations involve $h$-step multiplier
products.  At $h = 1$, the autocorrelation equals the multiplier
character sum (Theorem~\ref{thm:shift-one}), so VdC with a single
shift gives only $O(N)$.  Higher lags may decorrelate:

\begin{property}[\defname{HigherOrderDecorrelation} (\abbr{HOD})]
For every missing prime~$q$, non-trivial~$\chi$, and $\varepsilon > 0$:
there exists $H_0$ such that for $H \geq H_0$, $N_0$ such that for
$N \geq N_0$ and all $1 \leq h \leq H$:
$\|R_h(N)\| \leq \varepsilon N$.
\end{property}

\begin{theorem}[\lean{EM/LargeSieve/Spectral.lean}{hod\_implies\_ccsb}]
\label{thm:hod-mc}
$\mathrm{HOD} \Longrightarrow \mathrm{CCSB} \Longrightarrow \MC$.
\end{theorem}

\paragraph{Conditional multiplier equidistribution.}

\begin{property}[\defname{ConditionalMultiplierEquidist} (\abbr{CME})]
For every missing prime~$q$, non-trivial~$\chi$, $\varepsilon > 0$,
$N_0$ such that for $N \geq N_0$ and every
$c \in (\ZZ/q\ZZ)^\times$:
$\|\sum_{\substack{n < N \\ w(n) = c}} \chi(m(n))\| \leq \varepsilon N$.
\end{property}

\begin{theorem}[\lean{EM/CME/Reduction.lean}{cme\_implies\_dec}]
$\mathrm{CME} \Longrightarrow \mathrm{DecorrelationHypothesis}$.
\end{theorem}

\begin{theorem}[\lean{EM/CME/Reduction.lean}{cme\_implies\_ccsb}]
\label{thm:cme-ccsb}
$\mathrm{CME} \Longrightarrow \mathrm{CCSB}$.
\end{theorem}

\begin{proof}[Proof sketch]
The walk telescoping identity gives
$\sum \chi(w(n)) = \sum \chi(w(n))\chi(m(n)) - (\chi(w(N)) - \chi(w(0)))$.
The product sum decomposes by fiber:
$\sum \chi(w(n))\chi(m(n)) = \sum_a \chi(a) \cdot \sum_{w(n)=a} \chi(m(n))$.
CME bounds each fiber sum by $\varepsilon' N$; the triangle inequality
sums over at most $|(\ZZ/q\ZZ)^\times|$ fibers; the boundary term
$\chi(w(N)) - \chi(w(0))$ has norm~$\leq 2$ and is absorbed for large~$N$.
\end{proof}

This is the key reduction that bypasses PED, BRE, and the $d \geq 3$
barrier.  The proof works for all character orders because it uses
only the fiber decomposition and telescoping---no block rotation
estimate is needed.

\begin{theorem}[\lean{EM/CME/Reduction.lean}{cme\_implies\_mc}]
$\mathrm{CME} \Longrightarrow \MC$.
\end{theorem}

\begin{proof}
Compose \texttt{cme\_implies\_ccsb} with \texttt{complex\_csb\_mc'}.
\end{proof}

\begin{theorem}[\lean{EM/CME/Reduction.lean}{cme\_chain\_mc}]
$\mathrm{CME} + \mathrm{PEDImpliesCSB} \Longrightarrow \MC$.
\end{theorem}

This older route through the Dec $\to$ PED $\to$ CCSB chain is
superseded by the direct CME $\to$ CCSB reduction above, which
requires no additional hypotheses.

\subsubsection*{The Complete Hypothesis Hierarchy}

\[
\mathrm{PED} \;<\; \mathrm{Dec} \;<\; \mathrm{CME}
\;\xrightarrow{\text{proved}}\;
\mathrm{CCSB} \;\approx\; \mathrm{HOD}
\;\approx\; \mathrm{SVE},
\]
where ``$<$'' means a proved implication whose converse is not proved
for a fixed missing~$q$ (as global propositions PED, Dec, CME, CCSB, HOD, SVE
are all implied by MC vacuously, and CME, CCSB are equivalent to MC,
\lean{EM/CME/Equivalences.lean}{walk\_layer\_equivalences}) and
``$\approx$'' means equivalent.
HOD~$\Leftrightarrow$~CCSB via van der Corput;
SVE~$\Leftrightarrow$~CCSB via Parseval;
CME~$\Rightarrow$~CCSB via telescoping + fiber decomposition
(Theorem~\ref{thm:cme-ccsb}).

Every hypothesis implies MC.  The PED route has an open BRE bridge
for $d \geq 3$ characters, but this is now bypassed: the direct
CME~$\to$~CCSB arrow is proved for all character orders.  CME is the
\emph{sharpest reformulation}: the statement with the least analytic
content per missing prime that still implies MC (and, being quantified
over missing primes, is equivalent to it).

%% =========================================================================
\subsection{Dead Ends Catalogue}
\label{app:dead-ends}
%% =========================================================================

The catalogue below is complete: every numbered dead end of the
project, with the reason it fails.  Numbers are assigned progressively in
the session logs and are cited by number throughout the repository, so
they are never reused or renumbered; of the \DEnumbers~numbers,
\DEunassigned\ (\#25, \#64--\#72) were never assigned to an entry in any
log and are listed as such rather than back-filled, leaving
\DEentries~entries.  \DEwitnessed~of them carry a genuine formal Lean
witness (a theorem whose statement \emph{is} the counterexample, the
collapse, or the false claim---not a \code{True}-bodied marker), shown
with the {\color{leanink}\sffamily\footnotesize$\checkmark$} mark;
\DErevivable~have weak-MC revival score~$\geq 2$ (shown in brackets),
meaning they contribute to weaker forms of the conjecture despite being
dead for MC itself.  The single source of truth is
\code{tools/dead\_ends.tsv}; the table, the registry block in
\code{EM/Meta/DeadEnds.lean} (whose theorem \code{dead\_end\_registry}
fixes the counts) and \code{docs/dead\_ends\_catalog.md} are generated
from it.

The grouping is by \emph{failure mode}, and the failure modes are the
four requirements of the four-way blocker of \S\ref{sec:four-way} seen
from the failure side, refined:
\begin{itemize}[nosep]
\item \textbf{OS} orbit specificity --- population or ensemble statistics
  do not determine what one orbit does; the wall in front of every route
  (\#90);
\item \textbf{DG} decorrelation gap --- multiplier-level cancellation or
  independence does not transfer to the walk (\#58, \#117);
\item \textbf{AG} aggregate gap --- an aggregate or average-case bound
  does not give the per-fibre bound needed;
\item \textbf{CO} collapse --- the proposed hypothesis is definitionally
  (or by a short argument) equivalent to CME, CCSB, DH or MC itself;
\item \textbf{CI} circularity --- the argument presupposes its conclusion;
\item \textbf{SF} structurally false --- an explicit counterexample or a
  proved refutation of the proposed statement (including the three
  population-level layers refuted in 2026: \#156, \#157, \#160);
\item \textbf{TM} technique mismatch --- the tool needs independence,
  multiplicativity, stationarity or an algebraic family that the walk
  provably lacks (the largest class);
\item \textbf{SM} scale mismatch --- the tool's error term exceeds the
  signal on an $O(\log x)$-term or exponentially sparse orbit;
\item \textbf{MR} methodological rules --- \#1--\#3, standing rules of the
  project rather than mathematics.
\end{itemize}
Read that way the table is the evidence for the blocker, and the blocker
is the reason the table has so few kinds of entry.  Session numbers
(S$n$) date each entry; the aliases and numbering collisions found in the
reconstruction are recorded in the status column of the TSV and in the
registry's ``numbering aliases'' section.

\smallskip
\input{dead_ends_table.tex}

\noindent The congruence-obstruction family of dead ends
(\#142--\#145 and \#149--\#151: the mod-4 Cox--van der Poorten
extension, the collapse $\mathrm{IC}_{\min} \Leftrightarrow \mathrm{MC}$,
the reciprocity-transfer assessment, the decoupling of simultaneous
avoidance, the consumption ledger, and the covering-system and
confinement-cohomology obstructions) is treated in detail in
Section~\ref{sec:min-max-dichotomy}; the complete numbered catalogue
with witnesses and revival scores is the machine-checked registry in
\code{EM/Meta/DeadEnds.lean}.

\paragraph{The CRT-propagation route to the first moment (Dead
Ends~\#136--\#139), condensed.}
The route aimed to prove FirstMomentStep
(\lean{EM/Ensemble/FirstMoment.lean}{FirstMomentStep}), and hence
LMG and PositiveDensityRSD, from \textbf{CRTPropagationStep}
(\archived{EM/Archive/Ensemble/CRTArchive.lean}{CRTPropagationStep}): equidistribution
of $\Prod{n}(k) \bmod q$ propagating from step~$k$ to step~$k{+}1$,
seeded by SRE and converted to FMS via the Alladi--Buchstab weights.
CRTPropagationStep was further reduced to
\textbf{EnsembleTransitionApprox}~(ETA,
\archived{EM/Archive/Ensemble/BackwardDynamicsArchive.lean}{EnsembleTransitionApprox}),
following Tao's backward dynamics for the Collatz
conjecture~\cite{Tao2019Collatz}: the backward density decomposition
$F_{k+1}(a) = \sum_c F_k(c) \cdot R(c,a,k,X)$ with
$R = 1/(q{-}1) + O(1/\log X)$ by CRT-blindness of the multiplier
(\lean{EM/Group/CRT.lean}{crt\_multiplier\_invariance}), with the
reductions \code{eta\_dcta\_implies\_crt\_propagation} (deleted in the
Session-288 consolidation as pure RED-to-RED plumbing; see git history)
and
\archived{EM/Archive/Ensemble/BackwardDynamicsArchive.lean}{eta\_sre\_implies\_prsd}
machine-verified before archival.  The whole backward-dynamics file ---
joint counts, transition probabilities, probability axioms, and the
backward decomposition, all proved --- was retired to
\code{EM/Archive/Ensemble/BackwardDynamicsArchive.lean} (local archive) in the
Session-301 reorganization, since its only purpose was to feed this
chain.  The absorption mechanism kills the route: the correct
per-coprime-class density is $r/(r^2{-}1)$, not $1/(r{-}1)$, and even
that limit is never sustained for $k \geq 1$ because death events
drain nonzero-class mass permanently into the absorbing state
$0 \bmod q$, so AccumulatorEquidistPropagation---and with it
CRTPropagationStep and ETA---is false, and the proved chain
ETA+DCTA+SRE~$\Rightarrow$~PRSD is vacuously true.  The SMLB and
DeathDensityLB routes to the first moment
(\S\ref{sec:death-density}) are unaffected.

%% =========================================================================
\subsection{Methodology: Human--AI Collaboration}
\label{app:agents}
%% =========================================================================

This work was produced through a sustained collaboration between a
human author and an AI system (Claude, Anthropic) across more than 300~sessions.
The human author directed the mathematical strategy---proof
architecture, dead-end identification, and editorial control---while
the AI system handled Lean~4 formalization, Mathlib API search,
literature scouting, and exploration of candidate proof strategies.

The interaction was organized at scale via an \emph{agent swarm}:
a multi-agent system built on the Claude Agent SDK\@.  The swarm comprises
seven specialized agents, each with its own system prompt, tool access,
and model:

\begin{itemize}[nosep]
\item A \emph{coordinator} that reads the current proof state,
  selects the most promising action, dispatches specialists, and updates
  shared state files.
\item A \emph{formalizer} that writes and compiles Lean code
  in rapid iteration cycles.
\item A \emph{literature scout} that searches papers and Mathlib
  for relevant results.
\item Four \emph{attack vector specialists} focused on analytic,
  algebraic, combinatorial, and information-theoretic approaches.
\item A \emph{paper writer} that maintains this document (that is nevertheless often revised and maintained by the author).
\item A \emph{code stylist} whose goal is to improve the code quality and trying to enforce Mathlib conventions.
\end{itemize}

Agent prompts are \emph{self-evolving}: after each session the
coordinator updates them to record dead ends, new Mathlib discoveries,
and shifted priorities.  This prevents agents from rediscovering settled
territory.  All agent state (progress, strategy log, findings) is stored
as git-tracked markdown, making the exploration history fully
reproducible.

The division of labor between human and AI was sharp:

\begin{itemize}[nosep]
\item \textbf{Human:} mathematical direction, proof strategy,
  identification of dead ends, evaluation of intermediate results,
  architectural decisions on the reduction hierarchy, and editorial
  control over the final formalization and paper.
\item \textbf{AI (Claude):} Lean~4 formalization using Mathlib,
  Mathlib API search, literature scouting, exploration of candidate proof
  strategies, and drafting of this paper.
\end{itemize}

The human author guided the proof effort across more than 300~sessions, suggesting
attack vectors (algebraic, analytic, combinatorial, sieve-theoretic),
identifying when an approach had reached a dead end, and pushing toward
the sharpest possible reductions.  The AI agents wrote all Lean code,
searched Mathlib for relevant lemmas, explored dozens of proof strategies
to completion or refutation, and maintained the evolving paper.

The swarm is optimized for formalization and reduction, not mathematical
discovery.  The next breakthrough, if it comes, will probably be a human
insight about the structure of $\minFac$ on EM products---not something
an agent finds by systematic search.

%% =========================================================================
\subsection{Glossary of Definitions and Hypotheses}
\label{sec:glossary}
%% =========================================================================

The table below collects every named definition, hypothesis, and key
theorem introduced in this paper, with abbreviations and the section
where each is defined.

\medskip
\renewcommand{\arraystretch}{1.25}
\footnotesize
\begin{longtable}{@{}l l p{7.2cm} l@{}}
\toprule
\textbf{Abbr.} & \textbf{Name} & \textbf{Meaning} & \textbf{Ref.} \\
\midrule
\endfirsthead
\toprule
\textbf{Abbr.} & \textbf{Name} & \textbf{Meaning} & \textbf{Ref.} \\
\midrule
\endhead
\midrule
\multicolumn{4}{r}{\emph{continued on next page}} \\
\endfoot
\bottomrule
\endlastfoot

\multicolumn{4}{l}{\emph{Core sequence and walk}} \\
--- & Walk / Multiplier
    & $\walkZ{2}{q}{n} = \Prod{2}(n) \bmod q$;\; $\multZ{2}{q}{n} = \seq{2}(n\!+\!1) \bmod q$
    & Def.~\ref{def:walk-mult} \\
\abbr{MC} & MullinConjecture
    & Every prime appears in the Euclid--Mullin sequence
    & Conj.~\ref{conj:mullin} \\
\abbr{ANT} & ---
    & Standard analytic number theory (Dirichlet + sieve); its Mertens-form
      entry point is now proved (Karamata)
    & \S\ref{sec:ant-def} \\
\abbr{MFRE} & MinFacResidueEquidist
    & $\minFac$ residues equidist.\ mod $q$ among $q$-rough sqfree ints;
      \textbf{false} (head domination, Dead End~\#160); archived
    & \S\ref{sec:ant-def} \\
\midrule

\multicolumn{4}{l}{\emph{Algebraic hypotheses (\S\ref{sec:walk}--\S\ref{sec:bootstrap})}} \\
\abbr{SE} & SubgroupEscape
    & No proper subgroup of $(\ZZ/q\ZZ)^\times$ contains all multipliers
    & Def.~\ref{def:se} \\
\abbr{HH} & HittingHypothesis
    & The walk reaches $-1$ cofinally: $\forall N,\,\exists n \geq N,\; q \mid \Prod{2}(n)+1$
    & Def.~\ref{def:hh} \\
\abbr{DH} & DynamicalHitting
    & $\SE(q) \Rightarrow \HH(q)$ for every missing prime~$q$
    & Def.~\ref{def:dh} \\
\abbr{SHH} & SingleHitHypothesis
    & $\MC({<}\,q) + \SE(q) + q$ missing $\Rightarrow$ $\exists\, n \geq N_0$
      with $q \mid \Prod{2}(n)+1$
    & Def.~\ref{def:shh} \\
\abbr{PRE} & PrimeResidueEscape
    & Every proper subgroup of $(\ZZ/p\ZZ)^\times$ is escaped by some odd prime $< p$
    & Thm.~\ref{thm:pre} \\
$\abbr{PRE}_\ell$ & PowerResidueEscape
    & Multipliers escape the index-$\ell$ subgroup of $(\ZZ/q\ZZ)^\times$
    & \S\ref{sec:bootstrap} \\
--- & ThresholdHitting
    & DH restricted to primes $q \geq B$
    & \S\ref{sec:large-sieve} \\
\midrule

\multicolumn{4}{l}{\emph{Character-analytic hypotheses (\S\ref{sec:character})}} \\
\abbr{CCSB} & ComplexCharSumBound
    & Walk char sums $S_\chi(N) = o(N)$ for all non-trivial $\chi$
    & \S\ref{sec:character} \\
\abbr{MMCSB} & MultiModularCSB
    & CCSB simultaneously for all primes $q$ in a range
    & \S\ref{sec:character} \\
\abbr{ALS} & AnalyticLargeSieve
    & Large sieve inequality adapted to EM walk
    & \S\ref{sec:character} \\
--- & HilbertInequality
    & Montgomery--Vaughan generalized Hilbert inequality (IK Lemma~7.8)
    & App.~\ref{app:routes} \\
--- & CscPartialFraction
    & Mittag-Leffler expansion of $\pi\csc(\pi\theta)$ (proved)
    & App.~\ref{app:routes} \\
\abbr{PED} & PositiveEscapeDensity
    & Positive density of $n$ with $\multZ{2}{q}{n} \notin H$, for every proper $H$
    & \S\ref{sec:character} \\
--- & DecorrelationHypothesis
    & $\multZ{2}{q}{n}$ and $\multZ{2}{q}{n{+}1}$ are asymptotically independent
    & \S\ref{sec:character} \\
\abbr{BRE} & BlockRotationEstimate
    & Cancellation in block sums of characters applied to walk
    & \S\ref{sec:character} \\
\abbr{SVE} & SubquadraticVisitEnergy
    & $\sum_{a} |\{n \leq N : \walkZ{2}{q}{n}=a\}|^2 = o(N^2/(q{-}1))$
    & \S\ref{sec:character} \\
\abbr{SCD} & SelfCorrectingDrift
    & $R(N) = o(N^2)$; $\equiv$ SVE via Lyapunov telescope (Dead End~\#120)
    & \S\ref{sec:ensemble} \\
\abbr{TWD} & TailWindowDecorrelation
    & Avg.\ $|$cross term$|$ between windows vanishes; \textbf{false as stated}
      ($\chi \equiv 1$, Dead End \#156); centered version intended
    & \S\ref{sec:twd} \\
\abbr{HOD} & HigherOrderDecorrelation
    & Higher-order correlation bounds for walk increments
    & \S\ref{sec:character} \\
\abbr{CME} & ConditionalMultiplierEquidist
    & Conditional equidist.\ of multipliers given walk state; implies CCSB (proved)
    & \S\ref{sec:character} \\
--- & EMDirichlet
    & EM primes equidistributed in residue classes; alias for Dec
    & App.~\ref{app:routes} \\
--- & EMDImpliesCME
    & Open: unconditional equidist.\ $\Rightarrow$ conditional equidist.
    & App.~\ref{app:routes} \\
\abbr{WM} & WeakMullin
    & $\sum_{q \in M} 1/q < \infty$; MC $\Rightarrow$ WM $\Rightarrow$ RD (proved)
    & \S\ref{sec:weak-mullin} \\
\abbr{RD} & ReciprocalDivergence
    & $\sum_k 1/\seq{2}(k) = \infty$; WM $\Rightarrow$ RD (proved)
    & \S\ref{sec:weak-mullin} \\
\abbr{EMBV} & EM Bombieri--Vinogradov
    & Multi-modular char sum bound (alias for MMCSB); EMBV $\Rightarrow$ MC
    & \S\ref{sec:weak-mullin} \\
--- & JointSVE
    & Joint subquadratic visit energy; JointSVE $\Rightarrow$ MMCSB (proved)
    & \S\ref{sec:weak-mullin} \\
--- & PopulationEquidist
    & minFac equidist mod $q$ in shifted squarefree population; \textbf{false}
      (head domination, Dead End~\#160); archived with PopulationTransfer
    & \S\ref{sec:ant-def} \\
--- & ExcursionIndependence
    & Excursion char sums approximately independent; EIP + BME $\Rightarrow$ MC
    & App.~\ref{app:routes} \\
\midrule

\multicolumn{4}{l}{\emph{Ensemble framework (\S\ref{sec:ensemble})}} \\
\abbr{DSL} & DeterministicStabilityLemma
    & Retired: $\mathrm{PE} \Rightarrow \mathrm{CME}$, vacuous since PE is false
      (Dead End~\#160); archived together with DSLHitting
    & \S\ref{sec:dsl-route} \\
--- & genProd / genSeq
    & Generalized EM from arbitrary starting point $n$; genProd$(2,k) = \Prod{2}(k)$
    & \S\ref{sec:ensemble} \\
--- & EnsembleConcentration
    & Char energy density for bad starting points $\to 0$ as $X \to \infty$
    & \S\ref{sec:ensemble} \\
--- & CharSumVarianceBound
    & Ensemble average of char energy $\leq C \cdot K$ for all $X \geq X_0$
    & \S\ref{sec:ensemble} \\
--- & FiberEnergyBound
    & $\sum_a \|F(a,N)\|^2 \leq \varepsilon N^2$; FEB $\Rightarrow$ CME (proved)
    & \S\ref{sec:fiber-energy} \\
--- & JointStepEquidist
    & Joint distribution of consecutive genSeq equidist over sqfree $n$ (open)
    & \S\ref{sec:ensemble} \\
--- & MultCancelToWalkCancel
    & Mult.\ char cancel $\Rightarrow$ walk char cancel; $\equiv$~CCSB (Dead End \#117)
    & \S\ref{sec:ensemble} \\
--- & WeylHittingBridge
    & Walk char cancel $\Rightarrow$ cofinal hitting; proved
    & \S\ref{sec:ensemble} \\
--- & Cofactor Identity
    & $w(n){+}1 = m(n) \cdot \mathrm{cofZ}(n)$; proved but doesn't escape correlation
    & \S\ref{sec:telescope} \\
\midrule

\multicolumn{4}{l}{\emph{The composite floor (\S\ref{sec:cinfty})}} \\
\abbr{$(\omega 1)$} & PerpetualPrimality$(N)$
    & $\Prod{2}(n)+1$ prime for every $n \geq N$; the failure branch
    & Def.~\ref{def:perpetual-primality} \\
\abbr{(C$\infty$)} & InfinitelyManyComposite
    & $\Prod{2}(n)+1$ composite for infinitely many $n$; $=\lnot(\omega 1)$;
      implied by $\MC$, (V), $\mathrm{RD}$, and the obstruction programme; \textbf{open}
    & \S\ref{sec:cinfty-beneath} \\
\abbr{(S)} & Growth floor
    & If $f(n) \leq \seq{2}(n)$ eventually and $\sum 1/f$ converges then
      $\sum 1/\seq{2}$ converges; strictly stronger than (C$\infty$)
    & \S\ref{sec:growth-floor} \\
--- & Defect
    & $\delta(n) = \log\Prod{2}(n) - \log\seq{2}(n{+}1)$; measures the step
      against the maximal (doubling) step
    & \S\ref{sec:defect-telescope} \\
\abbr{$C$} & Growth constant
    & $C = \lim_N \log\Prod{2}(N)/2^{N} \geq 0$; a \emph{complete invariant}:
      $C = 0 \iff (\mathrm{C}\infty)$, $C > 0 \iff (\omega 1)$
    & Def.~\ref{def:growth-constant} \\
--- & Sylvester tower
    & Orbit of $s \mapsto s^2-s+1$ from $\Prod{2}(N)+1$; (C$\infty$) $=$ no
      such tower is all-prime
    & Thm.~\ref{thm:sylvester-tower} \\
\abbr{$q$} & Take-all walk
    & $q(w) = w^2+w$; simultaneously the branch residue walk, the take-all
      rule's walk, and (conjugated) the map governing (C$\infty$)
    & \S\ref{sec:level-tower} \\
--- & $\mathrm{PreZero}(\ell)$
    & Backward orbit of $0$ under $\Phi_6$ in $\ZZ/\ell\ZZ$; the (C$\infty$)
      target.  Level $k$ $=$ death of $q$ after $k$ steps; level $1$ is $\Phi_3$
    & \S\ref{sec:backward-orbit} \\
--- & $\Delta(z)$
    & $\Delta(z) = -3-16z$; the two discriminants one level above $z$ multiply
      to it.  $\Delta(-1) = 13$, $\Delta(\omega)\Delta(\omega^2) = 217$
    & \S\ref{sec:level-tower} \\
--- & Witness prime
    & Prime dividing $\Psi_k(\Prod{2}(N))$; always exists (Euclid), and
      (C$\infty$) $=$ some witness is a \emph{proper} factor
    & \S\ref{sec:arboreal} \\
\midrule

\multicolumn{4}{l}{\emph{Function field analog (\S\ref{sec:function-field})}} \\
\abbr{FF-MC} & FFMullinConjecture
    & Every monic irreducible appears in FF-EM sequence
    & \S\ref{sec:function-field} \\
--- & FFDynamicalHitting
    & Cofinal $Q \mid F(n){+}1$ for every monic irred.\ $Q$; FFDH $\Rightarrow$ FF-MC (proved)
    & \S\ref{sec:function-field} \\
--- & SelectionBiasNeutral
    & Min-degree selection preserves character cancellation (placeholder proposition,
      body \code{True}; the question is Dead End \#90 in FF form)
    & \S\ref{sec:function-field} \\
--- & FFSubgroupEscape
    & SE for $p \geq (d{-}1)^2{+}1$ via Weil contradiction (proved)
    & \S\ref{sec:function-field} \\
\midrule

\multicolumn{4}{l}{\emph{SDDS framework}} \\
--- & SDDS
    & Abstract sieve-defined dynamical system $s_{n+1} = s_n \cdot \Phi(s_n+1)$
    & App.~\ref{app:routes} \\
--- & NoAlgebraicObstruction
    & Multiplier subgroup generates full unit group; SE $\Rightarrow$ NAO (proved)
    & App.~\ref{app:routes} \\
--- & StrongSME
    & Walk hits every unit cofinally; StrongSME $\Rightarrow$ MC (proved)
    & App.~\ref{app:routes} \\
\midrule

\multicolumn{4}{l}{\emph{Named theorems}} \\
--- & Confinement
    & If SE fails, the walk is confined to a proper coset
    & Thm.~\ref{thm:confinement} \\
--- & Walk--Divisibility Bridge
    & $\walkZ{2}{q}{n} = -1 \Leftrightarrow q \mid \Prod{2}(n)+1$
    & Thm.~\ref{thm:bridge} \\
--- & One-prime gap
    & $\MC({<}\,q)$ + cofinal hit $\Rightarrow$ $\MC(q)$
    & Thm.~\ref{thm:one-prime-gap} \\
--- & Squarefree Accumulator
    & $\Prod{2}(n)$ is squarefree (product of distinct primes); proved
    & \S\ref{sec:weak-mullin} \\
--- & QR Obstruction
    & SE failure has density $O(2^{-k})$ by CRT + quadratic reciprocity
    & \S\ref{sec:bootstrap} \\
--- & Gauss sum inversion
    & Character sums $\leftrightarrow$ exponential sums via Gauss sums
    & \S\ref{sec:character} \\
\end{longtable}
\renewcommand{\arraystretch}{1.0}
\normalsize

